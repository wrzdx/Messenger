# Temporary HTTP-only ACME challenge server; do not expose auth over plain HTTP.
server {
    listen 80;
    server_name messenger.wrzdx.tech;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/messenger-acme;
    }
    location / {
        return 404;
    }
}
