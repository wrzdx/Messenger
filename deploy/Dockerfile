# Build the Linux binary locally; do not run the Go compiler on a small VPS.
FROM alpine:3.23.5
WORKDIR /app
RUN mkdir /app/logs && chown 10001:10001 /app/logs
COPY --chmod=0555 bin/messenger /app/messenger
USER 10001:10001
ENTRYPOINT ["/app/messenger"]
